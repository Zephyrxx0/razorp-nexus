# Quick Plan: Fix Onboarding Transact Snippet & Flexible Buyer Payload Parsing

## Description
The snippet generated during merchant onboarding in `src/app/dashboard/onboard/page.tsx` outputs flat `buyer_email` and `ip_address` fields, which were rejected with `422 Missing required intent or buyer object` by `/api/maas/:merchant_id/transact`. This plan normalizes incoming buyer payloads in the transact route (supporting nested `buyer`, `buyer_context`, and top-level flat fields), updates the onboarding snippet to canonical formatting, and ensures default onboarding test keys trigger mock payment capture.

## Tasks
1. Update `src/app/api/maas/[merchant_id]/transact/route.ts` to flexibly extract buyer context from `body.buyer`, `body.buyer_context`, or top-level `buyer_email` / `ip_address` / `ip`.
2. Update `src/app/dashboard/onboard/page.tsx` to generate canonical structured cURL snippets.
3. Update `nexus-agent/nexus_agent/razorpay_adapter.py` to recognize demo/onboarding keys (`rzp_test_Apex...`, devpass) for mock mode.
4. Verify by running the user's exact cURL snippet against `961d9807-5823-4fa8-ab19-edb2cdaeed17`.
