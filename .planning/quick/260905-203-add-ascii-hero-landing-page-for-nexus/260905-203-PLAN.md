# Quick Plan: Add Hero ASCII Landing Page for Nexus

## Description
Integrate the 21st.dev `hero-ascii` component (Vitruvian ASCII canvas shader) and build a complete, high-end fintech landing page for Nexus at `/`.

## Tasks
1. Implement `src/components/ui/ascii-vitruvian.tsx` with high-performance HTML canvas ASCII shader.
2. Implement `src/components/landing/hero-ascii.tsx` tailored to Nexus (MaaS gateway, cross-merchant Trust Graph, Razorpay test mode, Gemini 2.0 Flash).
3. Implement `src/app/page.tsx` integrating the ASCII hero with the 6-step deterministic commerce chain, dual-layer feature breakdown, live proof metrics, and developer curl terminal.
4. Verify with `npm run build` and `npm test`.

