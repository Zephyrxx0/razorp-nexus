---
status: complete
quick_id: 260905-203
date: 2026-09-05
description: "Add Hero ASCII landing page for Nexus"
---

# Quick Task Summary: Add Hero ASCII Landing Page for Nexus

## Summary
Adapted the 21st.dev `hero-ascii` component (Vitruvian ASCII canvas shader) and built an end-to-end, high-aesthetic cyberpunk fintech landing page for Nexus at `/`.

## Changes Made
1. **Created `src/components/ui/ascii-vitruvian.tsx`**:
   - Self-contained HTML5 `<canvas>` ASCII shader executing signed-distance field math (Da Vinci's Vitruvian Man, breathing circle, earthly square, and limb morphing).
   - Monospace grid sampling with character density ramps (`RAMP_BRIGHT`, `RAMP_DIM`, twinkling starfield).
   - Supports both `emerald` (Nexus brand signature) and `white` themes.
2. **Created `src/components/landing/hero-ascii.tsx`**:
   - Technical retro-futuristic HUD chrome with bracketed corner accents.
   - Header with brand mark, fintech coordinates (Bangalore / Razorpay HQ), and ASCII theme toggle.
   - Tailored product copy: "AUTONOMOUS COMMERCE", highlighting the dual-layer MaaS gateway and real-time Trust Graph.
   - Direct action buttons to Launch Console, Onboard Store, and Inspect Graph.
   - Bottom status bar with animated equalizer, live telemetry, and protocol specs.
3. **Created `src/components/ui/hero-ascii.tsx`**:
   - Re-exports `HeroAscii` for standard shadcn component path compatibility.
4. **Updated `src/app/page.tsx`**:
   - Renders the interactive `<HeroAscii />`.
   - Dual-Layer Architecture breakdown (Layer 1: MaaS Gateway, Layer 2: Cross-Merchant Trust Graph).
   - 6-Step Deterministic Execution Loop cards (`parse_intent`, `resolve_catalog`, `check_trust_graph`, `create_order`, `capture_payment`, `log_audit_entry`).
   - Copyable developer terminal showing sample agent `POST /api/maas/{merchant_id}/transact` request and JSON response.
   - Technical footer with direct navigation to dashboard views.

## Verification
- `npm run build`: Production build succeeded for all 21 routes with zero compilation or type errors.
- `npm test`: All 15 test suites and 106 tests passed.
