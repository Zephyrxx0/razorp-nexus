---
status: complete
quick_id: 260905-2d4
date: 2026-09-05
description: "Fix duplicate HeroAscii export and verify zero runtime errors"
---

# Quick Task Summary: Fix Duplicate HeroAscii Export

## Summary
Resolved the Next.js compilation error `Module parse failed: Duplicate export 'HeroAscii'` by eliminating duplicate exports and definitions across `src/components/landing/hero-ascii.tsx`, `src/components/ui/hero-ascii.tsx`, and `src/app/page.tsx`. Verified the application with Obscura headless browser and verified server logs and production builds.

## Changes Made
1. **`src/components/landing/hero-ascii.tsx`**:
   - Replaced duplicate component definition and appended export with a single clean re-export:
     `export { HeroSection as HeroAscii, HeroSection as default } from "./hero-section"`.
2. **`src/components/ui/hero-ascii.tsx`**:
   - Removed conflicting duplicate export lines, cleanly re-exporting `HeroSection` as `HeroAscii` and `default`.
3. **`src/app/page.tsx`**:
   - Removed duplicate import of `HeroAscii` and duplicate `<HeroAscii />` JSX invocation, ensuring single `<HeroSection />` rendering.

## Verification
- **Obscura Headless Browser (`obscura fetch http://localhost:3000/ --allow-private-network`)**:
  - Headless browser fetched and executed JavaScript on `http://localhost:3000/`.
  - DOM returned complete `<html lang="en" class="dark">` with `<title>NEXUS — Autonomous Agent Commerce & Trust Graph Platform</title>` and all technical HUD elements intact.
- **Route Status Verification**:
  - `/` -> `200 OK`
  - `/dashboard` -> `307 Temporary Redirect` (redirects to `/dashboard/transactions`)
  - `/dashboard/transactions` -> `200 OK`
  - `/dashboard/catalog` -> `200 OK`
  - `/dashboard/trust-graph` -> `200 OK`
  - `/dashboard/settings` -> `200 OK`
  - `/dashboard/onboard` -> `200 OK`
- **TypeScript Type Check (`npx tsc --noEmit`)**:
  - 0 type errors.
- **Test Suite (`npm test`)**:
  - 15 test suites passed (106 tests passed, 0 failures).
- **Production Build (`npm run build`)**:
  - All 21 static/dynamic pages compiled and optimized successfully with 0 errors.
