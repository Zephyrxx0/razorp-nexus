# Quick Plan: Fix Duplicate HeroAscii Export & Verify with Obscura

## Description
Fix the duplicate export error in `src/components/landing/hero-ascii.tsx` and `src/components/ui/hero-ascii.tsx`, ensure `src/app/page.tsx` cleanly renders `<HeroSection />` without duplication, and verify zero runtime errors via `obscura` and Next.js logs.

## Tasks
1. Clean up `src/components/landing/hero-ascii.tsx` to cleanly re-export `HeroSection` without duplicate definitions.
2. Clean up `src/components/ui/hero-ascii.tsx` to remove duplicate export statements.
3. Update `src/app/page.tsx` to render a single `<HeroSection />`.
4. Run `obscura fetch http://localhost:3000/ --allow-private-network` and verify browser status & console.
5. Check Next.js server logs and run `npm run build` and `npm test` to guarantee zero runtime regressions.
