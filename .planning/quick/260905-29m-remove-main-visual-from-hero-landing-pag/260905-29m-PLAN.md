# Quick Plan: Remove Main Visual from Hero Landing Page

## Description
Remove the main ASCII Vitruvian canvas visual from `src/components/landing/hero-ascii.tsx` as requested by the user, replacing it with a clean, unobtrusive dark cyber-grid/ambient glow backdrop so the typography, technical framing, telemetry HUD, and CTAs remain pristine and unobstructed.

## Tasks
1. Update `src/components/landing/hero-ascii.tsx`:
   - Remove `<AsciiVitruvian />` canvas component import and invocation.
   - Remove the theme switcher toggle from the header.
   - Introduce subtle ambient technical grid and radial glow backdrop that does not overlap or obscure the hero text.
2. Verify `npm run build` and `npm test` pass.
